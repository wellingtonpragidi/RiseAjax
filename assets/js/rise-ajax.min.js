const RiseAjax ={_lastForm:null,request:function(options){const xhr = new XMLHttpRequest;if(! options.url){console.error('URL nao definida');return;}xhr.open(options.method || 'GET',options.url,true);let data = options.data;let contentType = options.contentType || null;if(! contentType){if(data instanceof FormData){contentType = 'multipart/form-data';// Para formularios com arquivos}else if(typeof data === 'object'){contentType = 'application/json';data = JSON.stringify(data);// Converte objeto para JSON}else if(typeof data === 'string' && data.includes('=')){contentType = 'application/x-www-form-urlencoded';}}if(contentType){xhr.setRequestHeader('Content-Type',contentType);}if(options.headers){for(const[key,value]of Object.entries(options.headers)){xhr.setRequestHeader(key,value);}}xhr.addEventListener('load',function(){if(xhr.status >= 200 && xhr.status < 300){if(options.success){options.success(xhr.responseText);}}else{if(options.error){options.error(xhr.statusText);}}if(options.complete){options.complete();}});xhr.send(data);},form:function(selector,options){const form = document.querySelector(selector);if(! form){return console.error('Form nao encontrado:',selector);}this._lastForm = form;form.addEventListener('submit',(e)=>{if(options.preventDefault !== false){e.preventDefault();}const formData = new FormData(form);let data;let contentType = options.contentType || null;if(contentType === 'application/json'){data = Object.fromEntries(formData);// Converte FormData para objeto}else if(contentType === 'multipart/form-data'){data = formData;// Mantem como FormData}else{data = new URLSearchParams(formData).toString();contentType = 'application/x-www-form-urlencoded';}this.request({url:options.url || form.action,method:options.method || form.method || 'POST',data,contentType,// Passa o contentType para o request headers:options.headers,// Headers adicionais before:options.before,success:options.success,error:options.error,complete:options.complete});});},clearFields:function(){if(! this._lastForm){console.warn('Nenhum formulario foi registrado. Use RiseAjax.form()primeiro.');return;}let formElement = this._lastForm.querySelectorAll('input,textarea,select');formElement.forEach(field =>{if(field.type === 'checkbox' || field.type === 'radio'){field.checked = false;}else if(field.tagName === 'SELECT'){field.selectedIndex = 0;}else{field.value = '';}});},getElement:(selector)=>{const element =(typeof selector === 'string')? document.querySelector(selector):selector;if(! element){console.error('Elemento nao encontrado:',selector);}return element;},appear:function(selector,duration = 1000){const element = this.getElement(selector);if(! element){return;}element.style.opacity = '0';element.style.display = 'block';let start = null;const animate =(timestamp)=>{if(! start){start = timestamp;}const elapsed = timestamp - start;element.style.opacity = Math.min(elapsed / duration,1);if(elapsed < duration){requestAnimationFrame(animate);}};requestAnimationFrame(animate);return this;},disappear:function(selector,duration = 1000){const element = this.getElement(selector);if(! element){return;}let start = null;const animate =(timestamp)=>{if(! start)start = timestamp;const elapsed = timestamp - start;element.style.opacity = Math.max(1 - elapsed / duration,0);if(elapsed < duration){requestAnimationFrame(animate);}else{element.style.display = 'none';}};requestAnimationFrame(animate);return this;},swapClass:function(selector,oldClass,newClass){const element = this.getElement(selector);if(! element || ! element.classList){return;}element.classList.remove(oldClass);element.classList.add(newClass);}};
